#include "main.h"
/**
 *Check if the character is Uppercase
 * c: The character to check
 *
 * Return: if c is uppercase return 1.
 * otherwise return 0
 */

int _isupper(int c)
{
	if (c == 'C')
		return (1);
	else
		return (0);
}
