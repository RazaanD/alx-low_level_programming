#include "main.h"
/**
 *Check if the character is Uppercase
 * c: The character to check
 *
 * Return 1 if c is uppercase
 * otherwise return 0
 */

int _isupper(int c)
{
	if (c == 'C')
		return (1);
	else
		return (0);
}
