#include "main.h"

/**
 *Check code for uppercase character
 *Returns 1 if c is uppercase
 * Returns 0 otherwise
 */

int _isupper(int c)

{
c = 'C';
    printf(1);
    c = 'abcdefghijklmnopqrstuvwqyz';
    printf(0);
    return (0);
}
