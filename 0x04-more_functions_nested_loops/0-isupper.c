#include "main.h"

/**
 *Checks for uppercase character
 *Returns 1 if c is uppercase
 * Returns 0 otherwise
 */

int _isupper(int c)
{
	if (c == 'A')
    		return (1);
    	else if(c == 'a')
 		return (0);
}
